#pragma once

#include <string>
#include <iostream>
#include <iterator>

namespace ft
{
	class ALocation
	{
		protected:
			// std::string _root;
			// std::string _index;
			// std::string _uploadPath;
			// bool _isGet;
			// bool _isPost;
			// bool _isDelete;
			// bool _autoIndex;

		public:
			ALocation();
			virtual ~ALocation();
	};
}
